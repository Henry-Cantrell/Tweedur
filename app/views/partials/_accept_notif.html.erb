<%= simple_form_for :accept_notif, url: '/notifications', method: 'post' do |f| %>
    <%= f.hidden_field :id, name: :id, value: follow_request.id %>
    <%= f.button :submit, 'Accept', class: "button is-success is-small is-outlined"%>
<% end %>